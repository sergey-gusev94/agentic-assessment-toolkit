# Pinned Codex grading configuration selecting the `content-v2` rubric
# revisions — the administrative-language cleanup of rubrics whose
# frozen `default.md` scored or gated on identity, signatures, or
# submission formalities (docs/data-conventions.md, "Rubrics enumerate
# academic content only"). Use with runs scoped to assignments that
# have a `content-v2.md`; other assignments fail at plan time by
# design, since rubric selection is per-config.
stage = "grade"
agent = "codex"
model = "openai/gpt-5.6-sol"
reasoning_effort = "high"
prompt = "grader"
rubric = "content-v2"
